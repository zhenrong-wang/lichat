name: "Windows | MSVC++ | x64 | Debug"

on:
  push:
    branches: [ "feature/cross-platform-and-dependencies" ]
    paths:
      - '**.cpp'
      - '**.hpp'
      - '**/CMakeLists.txt'
      - '.github/workflows/build_and_test_windows_msvc_x64_debug.yml'
      - 'CMakePresets.json'
  pull_request:
    branches: [ "feature/cross-platform-and-dependencies" ]
    paths:
      - '**.cpp'
      - '**.hpp'
      - '**/CMakeLists.txt'
      - '.github/workflows/build_and_test_windows_msvc_x64_debug.yml'
      - 'CMakePresets.json'

jobs:
  build:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - uses: ilammy/msvc-dev-cmd@v1
      - uses: lukka/run-vcpkg@v11.5
      
      - name: Resolve dependencies and configure
        run: cmake --preset=x64-windows-debug

      - name: Build
        run: cmake --build --preset=x64-windows-debug